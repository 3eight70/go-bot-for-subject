### Используемые промпты:

**Первый промпт:**

```
Необходимо: 
1) целиком переписать бота из данного проекта с языка Python на язык Go. Ты можешь напрямую редактировать файлы. Если в боте где-то используется соединение с бд перепиши эти части кода на хранение данных в памяти. 
2) После переписывания на Go, требуется создать Dockerfile и docker-compose для развертывания бота в контейнере 
3) После выполнения первых 2ух шагов необходимо покрыть бота автотестами, также на Go и проверить, что они исполняются корректно 
4) Отметить комментариями, куда нужно задавать API токен для бота
 
Код бота представлен ниже:
// Тут мог быть ваш код
```

**Второй промпт:**

```
При попытке импорта зависимости выдает ошибку go: github.com/go-telegram-bot-api/telegram-bot-api/v5@v5.5.1: missing go.sum entry for go.mod file; to add it: go mod download github.com/go-telegram-bot-api/telegram-bot-api/v5
```

**Третий промпт:**

```
При попытке билда приложения выдает ошибку
 
tgbotapi.KeyboardButtonRow
```
На этот промпт он ответил маленьким куском кода, поэтому попросил написать файл целиком

`Полностью напиши main.go с твоими исправления`

**Четвертый промпт:**

```
Написанный тобой тест:
// Тут код теста

Падает со следующей ошибкой
// Текст ошибки
```

### Что правил ручками

Ручками исправлял только используемую версию GO, т.к. забыл ее специфицировать